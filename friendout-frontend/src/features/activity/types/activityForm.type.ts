/** Erreurs inline affichées sous chaque champ du formulaire d'activité. */
export interface FormErrors {
    title?: string
    description?: string
    startAt?: string
    time?: string
    localisation?: string
}
