# ─── OS & editors ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.suo
*.sw?

# JetBrains IDEs
.idea/

# VS Code (keep extensions.json, ignore everything else)
.vscode/*
!.vscode/extensions.json

# ─── Secrets / environment ───────────────────────────────────────────────────
.env.docker
.env.local

# ─── Docker ──────────────────────────────────────────────────────────────────
# (docker-compose.yml is versioned; only override files are ignored)
docker-compose.override.yml
